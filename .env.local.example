# Supabase local development settings
# Copy this file to .env.local and DO NOT commit .env.local to version control

# Supabase API URL (local development default)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Supabase Anon (publishable) Key
# IMPORTANT: Use only the anon/publishable key in frontend code, NEVER the secret key
# Get this from: npx supabase status
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Site URL for auth redirects (optional for local development)
# For local development, this defaults to http://localhost:3000
# For production, set this to your production URL (e.g., https://your-app.vercel.app)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# OpenAI API Key for AI recipe parsing
# Get this from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Mistral API Key for OCR (photo to recipe)
# Get this from: https://console.mistral.ai/api-keys/
MISTRAL_API_KEY=your_mistral_api_key_here

# Note: When running the app, use http://localhost:3000 for consistent auth redirects
# The Supabase auth allowlist includes both localhost:3000 and 127.0.0.1:3000
